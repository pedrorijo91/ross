#<%= idx %> <%= lang %>

<% lang_image = lang.downcase + "-lang-icon.svg"

   if ActionController::Base.helpers.resolve_asset_path(lang_image) %>
  <%= image_tag(lang_image, width: '20px') %>

<% end %>

<br/>