<p>Top languages:</p>

<!-- TODO table? -->
<% stats.languages.sort_by {|_key, value| value}.reverse[0..9].each_with_index do |lang, idx| %>
  <%= render partial: 'lang_line', :locals => {:idx => idx + 1, :lang => lang[0]} %>
<% end %>

<% (10 - stats.languages.size).times do |index| %>
  <%= render partial: 'lang_line', :locals => {:idx => stats.languages.size + index + 1, :lang => '-'} %>
<% end %>